{
  "name": "focal-point-picker",
  "version": "1.4.1",
  "type": "module",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "format:assets": "npx prettier --write '**/*.{js,css}'",
    "format:php": "./vendor/bin/pint",
    "analyse": "./vendor/bin/phpstan analyse --memory-limit=2G",
    "validate:php": "bin/cli.js validate:php",
    "version": "pnpm run validate:php && pnpm changeset version && bin/cli.js version:patch"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.11",
    "fast-glob": "^3.3.3",
    "husky": "^9.1.1",
    "lint-staged": "^15.2.7",
    "picocolors": "^1.1.1",
    "prettier": "^3.3.3"
  }
}
